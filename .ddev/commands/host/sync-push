#!/bin/bash
set -e

echo "🚀 Recupero modifiche da GitHub..."
git pull origin main --rebase || echo "⚠️ Nessuna modifica online o conflitto gestibile"

echo "📝 Aggiunta file e commit..."
git add .
MSG=${1:-"Sync da $(hostname) - $(date +'%d/%m/%Y %H:%M')"}
git commit -m "$MSG" || echo "⚪ Nulla di nuovo"

echo "📤 Upload su GitHub..."
git push origin main
